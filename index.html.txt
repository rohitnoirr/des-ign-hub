<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Rohit Noir — Design Hub</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      background-color: #000;
      color: #fff;
      font-family: 'Segoe UI', sans-serif;
      line-height: 1.6;
    }

    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px 40px;
      border-bottom: 1px solid #333;
    }

    .logo {
      font-size: 1.4rem;
      letter-spacing: 2px;
      text-transform: uppercase;
      color: #e50914;
    }

    nav a {
      color: #aaa;
      margin-left: 30px;
      text-decoration: none;
      transition: color 0.3s ease;
    }

    nav a:hover {
      color: #fff;
    }

    section {
      padding: 60px 40px;
      animation: fadeIn 1s ease-in;
    }

    h1 {
      font-size: 2.5rem;
      margin-bottom: 20px;
    }

    .upload-box, .rate-box, .feedback-box {
      background: #111;
      border: 1px solid #222;
      padding: 20px;
      margin-top: 20px;
      border-radius: 8px;
      transition: transform 0.3s ease;
    }

    .upload-box:hover, .rate-box:hover, .feedback-box:hover {
      transform: translateY(-5px);
    }

    input[type="file"], textarea, select, button {
      margin-top: 10px;
      width: 100%;
      padding: 10px;
      background: #222;
      border: none;
      color: #fff;
      border-radius: 4px;
      font-size: 1rem;
    }

    button {
      background: #e50914;
      cursor: pointer;
      transition: background 0.3s ease;
    }

    button:hover {
      background: #b00610;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body>

  <header>
    <div class="logo">Rohit Noir</div>
    <nav>
      <a href="#home">Home</a>
      <a href="#upload">Upload</a>
      <a href="#rate">Rate</a>
      <a href="#feedback">Feedback</a>
    </nav>
  </header>

  <section id="home">
    <h1>Welcome to the Noir Design Hub</h1>
    <p>Upload your work. Rate others. Build the future of design.</p>
  </section>

  <section id="upload">
    <h1>Upload Your Design</h1>
    <div class="upload-box">
      <p>Select your design file (JPG, PNG, PDF):</p>
      <input type="file" />
      <button>Submit</button>
    </div>
  </section>

  <section id="rate">
    <h1>Rate Other Designs</h1>
    <div class="rate-box">
      <p>Design Name: Sample Artwork</p>
      <label for="rating">Your Rating:</label>
      <select id="rating">
        <option>5 - Excellent</option>
        <option>4 - Good</option>
        <option>3 - Average</option>
        <option>2 - Poor</option>
        <option>1 - Bad</option>
      </select>
      <button>Submit Rating</button>
    </div>
  </section>

  <section id="feedback">
    <h1>Leave Feedback</h1>
    <div class="feedback-box">
      <textarea rows="5" placeholder="Write your feedback here..."></textarea>
      <button>Send Feedback</button>
    </div>
  </section>

</body>
</html>